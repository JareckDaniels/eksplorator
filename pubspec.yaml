name: eksplorator
description: Prosty eksplorator plikow
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  path_provider: ^2.1.4
  permission_handler: ^11.3.1
  archive: ^3.6.1
  open_filex: ^4.5.0
  share_plus: ^10.0.2
  shared_preferences: ^2.3.2
  intl: ^0.19.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
